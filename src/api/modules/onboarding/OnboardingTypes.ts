export type InstagramVerification = {
    id: number,
    user_id: string,
    verification_code: string,
    follower_count: number,
    verified: boolean,
    sender_id: string,
    handle: string
}