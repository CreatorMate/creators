
export const appSettings = {
    unGuardedRoutes: [
        "/login",
        "/auth0/callback",
        /^\/invitation\/[^/]+\/[^/]+$/,
    ],
    defaultLimit: 10,
    baseUrl: '',
    toastDuration: 3000
}